[general]
state_file = /var/awslogs/state/agent-state

[/var/log/bastion]
log_group_name = {{ cloudwatch_log_group_name }}
log_stream_name = {instance_id}-ssh-sessions
datetime_format = %b %d %H:%M:%S
multi_line_start_pattern = ^<13>[^\s]
file = {{ bastion_logfile }}
